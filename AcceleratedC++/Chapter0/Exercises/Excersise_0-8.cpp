#include <iostream>

// Works because // does not need  an ending delimiter
int main()
{
  // this is a comment that extends over several lines
  // by using // at the beginning of each line insead of /*
  // or */ to delimit comments

  std::cout << "Does this work?" << std::endl;

  return 0;
}